server:
  port: 8002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mytest
    username: root
    password: password
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: dev